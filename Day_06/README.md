# Day 06 — Collections Framework (List, Set)

This repository contains simple examples of Java Collections Framework focusing on **List** (ArrayList, LinkedList) and **Set** (HashSet, LinkedHashSet, TreeSet).

## 📌 Contents

- `ListExamples.java` — List operations (add, remove, iterate)
- `SetExamples.java` — Set operations (unique elements, ordering)
- LinkedIn short note for Day 06 in `/notes/day06-linkedin-short.txt`

---

## ▶ How to Compile & Run

### Compile all Java files:

```bash
javac -d out $(find src -name "*.java")
